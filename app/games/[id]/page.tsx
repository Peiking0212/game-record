import type { Metadata } from "next";
import { GalleryClient } from "@/components/gallery/gallery-client";

export const metadata: Metadata = { title: "濯掍綋搴? };

export default function GalleryPage() {
  return <GalleryClient />;
}
